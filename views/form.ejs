<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Blog</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <nav class="navbar-main">
        <div class="navbar-inner">
            <a href="/blogs" class="brand-main">MyBlog</a>
            <div class="nav-links-main">
                <a href="/blogs">Home</a>
                <% if (isAuthenticated) { %>
                    <a href="/blogs/add" class="active">Add Blog</a>
                    <div class="profile-dropdown">
                        <button class="profile-btn" onclick="toggleDropdown(event)">
                            <span class="profile-username">
                                <%= username %>
                            </span>
                            <svg height="20" width="20" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd"
                                    d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"
                                    clip-rule="evenodd" />
                            </svg>
                        </button>
                        <div class="dropdown-content" id="profileDropdown">
                            <a href="/logout">Logout</a>
                        </div>
                    </div>
                    <% } %>
            </div>
        </div>
    </nav>
    <div class="form-shell">
        <h1 style="margin-bottom: 24px;">Add New Blog</h1>
        <form action="/blogs/add" method="POST" enctype="multipart/form-data">
            <div class="form-field">
                <label for="title" class="label-form">Title</label>
                <input type="text" id="title" name="title" class="input-form" required>
            </div>
            <div class="form-field">
                <label for="content" class="label-form">Content</label>
                <textarea id="content" name="content" rows="6" class="input-form" required></textarea>
            </div>
            <div class="form-field">
                <label for="image" class="label-form">Image</label>
                <input type="file" id="image" name="image" class="input-form" accept="image/*" required>
            </div>
            <button type="submit" class="btn-form">Add Blog</button>
        </form>
        <a href="/blogs" class="link-form" style="display: block; text-align: center; margin-top: 20px;">Back to
            Blogs</a>
    </div>
    <footer class="footer-main">
        <p>&copy; 2024 MyBlog. All rights reserved.</p>
    </footer>
    <script>
        function toggleDropdown(event) {
            event.stopPropagation();
            var dropdown = document.getElementById('profileDropdown');
            dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
        }
        document.addEventListener('click', function () {
            var dropdown = document.getElementById('profileDropdown');
            if (dropdown) dropdown.style.display = 'none';
        });
    </script>
</body>

</html>